name: test
on:
  push:
    branches: [ main ]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: pip install pytest  # 让机器人安装 pytest 测试工具
      - run: pytest              # 让机器人自动寻找并运行所有 test_ 开头的测试文件
